import { createPrimitiveComponent } from "../shared/createPrimitiveComponent";

export const SidebarMenu = createPrimitiveComponent({
  primitive: "ul",
  baseClass: "flex flex-col gap-1",
  displayName: "SidebarMenu",
});
