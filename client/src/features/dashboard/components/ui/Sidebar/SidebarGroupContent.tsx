import { createPrimitiveComponent } from "../shared/createPrimitiveComponent";

export const SidebarGroupContent = createPrimitiveComponent({
  primitive: "div",
  baseClass: "flex flex-col gap-1",
  displayName: "SidebarGroupContent",
});
