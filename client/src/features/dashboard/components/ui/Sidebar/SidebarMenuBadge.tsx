import { createPrimitiveComponent } from "../shared/createPrimitiveComponent";

export const SidebarMenuBadge = createPrimitiveComponent({
  primitive: "span",
  baseClass: "ml-auto rounded-md bg-sidebar-accent px-2 py-0.5 text-xs",
  displayName: "SidebarMenuBadge",
});
