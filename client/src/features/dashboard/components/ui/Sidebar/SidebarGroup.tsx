import { createPrimitiveComponent } from "../shared/createPrimitiveComponent";

export const SidebarGroup = createPrimitiveComponent({
  primitive: "div",
  baseClass: "relative flex flex-col gap-1",
  displayName: "SidebarGroup",
});
