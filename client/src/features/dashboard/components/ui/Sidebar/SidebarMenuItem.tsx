import { createPrimitiveComponent } from "../shared/createPrimitiveComponent";

export const SidebarMenuItem = createPrimitiveComponent({
  primitive: "li",
  baseClass: "relative",
  displayName: "SidebarMenuItem",
});
