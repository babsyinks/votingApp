import { createPrimitiveComponent } from "../shared/createPrimitiveComponent";

export const SidebarFooter = createPrimitiveComponent({
  primitive: "div",
  baseClass: "flex flex-col gap-2 p-2",
  displayName: "SidebarFooter",
});
