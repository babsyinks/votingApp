import { createPrimitiveComponent } from "../shared/createPrimitiveComponent";

export const FormDescription = createPrimitiveComponent({
  primitive: "p",
  baseClass: "text-sm text-muted-foreground",
  displayName: "FormDescription",
});
