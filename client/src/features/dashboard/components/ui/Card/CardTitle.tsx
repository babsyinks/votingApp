import { createHeadingComponent } from "../shared/createHeadingComponent";

export const CardTitle = createHeadingComponent(
  "h3",
  "CardTitle",
  "text-2xl font-semibold leading-none tracking-tight",
);
